{
  "name": "force11",
  "private": true,
  "scripts": {
    "start": "npm --prefix frontend run preview -- --host 0.0.0.0 --port ${PORT:-4173}",
    "build": "npm --prefix frontend ci --omit=optional --no-audit --no-fund && npm --prefix frontend run build"
  }
}
