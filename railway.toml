[build]
builder = "NIXPACKS"
buildCommand = "npm --prefix backend ci --omit=dev --no-audit --no-fund"

[deploy]
startCommand = "npm --prefix backend start"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
